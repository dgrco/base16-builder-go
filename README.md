# Base16 Builder

A Go implementation of a Base16 Builder that follows the guidelines at Base16.

## Installation

```bash
git clone https://github.com/mxilinas/base16-builder
cd base16-builder
go build -o base16-builder
```

## Usage

```bash
cat scheme.yaml | ./base16-builder --template template.mustache > theme.file
```
